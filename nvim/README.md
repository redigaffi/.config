TBD

1. Install ansible
```
pip install ansible

```

2. Run playbook
```
ansible-playbook ansible-playbook.yaml -K
```

If any issues with Treesitter, uninstall all parsers with:
```
:TSUninstall all
```
